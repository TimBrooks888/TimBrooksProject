# fun_py_sort_by_length.py

def fun_py_sort_by_length(lst):
    return sorted(lst, key=len)

def test_sort_by_length():
    words = ["apple", "banana", "kiwi", "grape"]
    print(f"Sorted by length: {fun_py_sort_by_length(words)}")

if __name__ == "__main__":
    test_sort_by_length()
